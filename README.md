# rougether-landing

루게더(Rougether) 랜딩페이지 — https://triples-soma.github.io/rougether-landing/

- 정적 HTML 단일 페이지. `main` 푸시가 곧 배포(GitHub Pages).
- 색상은 앱의 cozy 테마 토큰(`rougether-mobile` `src/constants/theme.ts`)을 CSS 변수로 옮긴 것 — 토큰이 바뀌면 `:root` 블록을 함께 갱신.
- TODO: 히어로의 스크린샷 플레이스홀더를 실제 앱 캡처로 교체, 스토어 배지를 출시 후 실제 링크로 교체, 커스텀 도메인(CNAME) 결정.
